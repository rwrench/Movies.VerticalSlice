<UserControl x:Class="Movies.VerticalSlice.Api.Wpf.Views.RatingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             Height="Auto" Width="Auto">
    <Grid>
        <telerik:RadGridView ItemsSource="{Binding Ratings}" AutoGenerateColumns="False" IsReadOnly="True" 
                             CanUserFreezeColumns="False" CanUserReorderColumns="False" GroupRenderMode="Flat"
                             CanUserDeleteRows="True" CanUserInsertRows="True" ShowGroupPanel="False">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="Movie" DataMemberBinding="{Binding MovieName}" Width="*" />
                <telerik:GridViewDataColumn Header="Rating" DataMemberBinding="{Binding Rating}" Width="*" />
                <telerik:GridViewDataColumn Header="User" DataMemberBinding="{Binding UserName}" Width="*" />
                <telerik:GridViewDataColumn Header="Date Updated" DataMemberBinding="{Binding DateUpdated}" Width="*" />
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
    </Grid>
</UserControl>
