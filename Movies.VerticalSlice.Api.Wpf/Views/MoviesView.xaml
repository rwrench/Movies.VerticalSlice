<UserControl x:Class="Movies.VerticalSlice.Api.Wpf.Views.MoviesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             Height="Auto" Width="Auto">
    <Grid>
        <telerik:RadGridView ItemsSource="{Binding Movies}" AutoGenerateColumns="False"
                             CanUserFreezeColumns="False" CanUserReorderColumns="False" GroupRenderMode="Flat"
                             CanUserDeleteRows="True" CanUserInsertRows="True" ShowGroupPanel="False">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="Title" DataMemberBinding="{Binding Title}" Width="Auto" />
                <telerik:GridViewDataColumn Header="Release Year" DataMemberBinding="{Binding YearOfRelease}" />
                <telerik:GridViewDataColumn Header="Genres" DataMemberBinding="{Binding Genres}" />
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
    </Grid>
</UserControl>
