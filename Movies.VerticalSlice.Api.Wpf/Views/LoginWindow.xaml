<UserControl x:Class="Movies.VerticalSlice.Api.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Movies.VerticalSlice.Api.Wpf.Helpers"
         xmlns:prism="http://prismlibrary.com/">
    <StackPanel Margin="20">
        <TextBlock Text="Email:" />
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
        <TextBlock Text="Password:" />
        <PasswordBox Margin="0,0,0,10" 
                local:PasswordBoxHelper.BindPassword="True"
                local:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Margin="0,0,0,10">
             <Hyperlink Command="{Binding ForgotCommand}">
                 Forgot Username or Password?
             </Hyperlink>
        </TextBlock>
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,10"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Login" Command="{Binding LoginCommand}" IsDefault="True" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="10,0,0,0"/>
        </StackPanel>
    </StackPanel>
</UserControl>