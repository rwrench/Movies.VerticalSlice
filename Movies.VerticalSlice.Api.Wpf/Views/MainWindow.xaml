<Window x:Class="Movies.VerticalSlice.Api.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
         xmlns:views="clr-namespace:Movies.VerticalSlice.Api.Wpf.Views"
        Title="{Binding Title}" Height="350" Width="525" >
    <DockPanel>
        <!-- Sidebar Menu -->
        <StackPanel DockPanel.Dock="Left" Width="Auto" Background="#EEE">
            <Button Content="Login" Command="{Binding LoginCommand}" IsDefault="True" Margin="5"/>
            <Button Content="Movies" Command="{Binding NavigateCommand}" CommandParameter="MoviesView" Margin="5"/>
            <Button Content="Ratings" Command="{Binding NavigateCommand}" CommandParameter="RatingsView" Margin="5"/>
            <Button Content="Exit" Command="{Binding ExitCommand}" Margin="5"/>
        </StackPanel>
        <!-- Main Content -->
        <ContentControl prism:RegionManager.RegionName="MainRegion" />
    </DockPanel>
</Window>
